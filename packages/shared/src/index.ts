/**
 * Shared utilities used by both the Electron main process and the renderer.
 *
 * Currently empty — add logger, typed-event emitter, window-state persistence,
 * etc. here as the project grows.
 */
export {};