/**
 * Renderer-side re-export of the data-engine contract.
 * Import from `@contracts` (alias for ../contracts) in renderer code so the
 * Vue build can resolve it without needing the @notesnook-vue/contracts workspace
 * package at build time.
 */
export * from "@notesnook-vue/contracts";